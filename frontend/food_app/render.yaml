services:
  - type: web
    name: food-frontend
    rootDir: frontend/food_app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    buildFilter:
      paths:
        - frontend/food_app/**/*
    envVars:
      - key: NODE_ENV
        value: production
